replicaCount: 2
image:
  repository: salmonstone/infragpt
  pullPolicy: Always
  tag: "v1"
namespace: infragpt
secret:
  name: infragpt-secrets
  groqApiKeyRef: GROQ_API_KEY
  jwtSecretRef: JWT_SECRET_KEY
  postgresPasswordRef: POSTGRES_PASSWORD
postgres:
  enabled: true
  image: postgres:15-alpine
  user: infragpt
  database: infragpt
  storage: 2Gi
  storageClassName: gp2-immediate
  resources:
    requests:
      cpu: "100m"
      memory: "256Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
redis:
  enabled: true
  image: redis:7-alpine
  resources:
    requests:
      cpu: "50m"
      memory: "64Mi"
    limits:
      cpu: "200m"
      memory: "256Mi"
service:
  type: ClusterIP
  port: 80
  targetPort: 5000
ingress:
  enabled: true
  className: nginx
  host: www.infragpt.online
  tls:
    enabled: true
resources:
  app:
    requests:
      cpu: "150m"
      memory: "256Mi"
    limits:
      cpu: "600m"
      memory: "768Mi"
  filebeat:
    requests:
      cpu: "50m"
      memory: "64Mi"
    limits:
      cpu: "100m"
      memory: "128Mi"
autoscaling:
  enabled: true
  minReplicas: 2
  maxReplicas: 6
  targetCPUUtilizationPercentage: 60
  targetMemoryUtilizationPercentage: 70
podDisruptionBudget:
  enabled: true
  minAvailable: 1
filebeat:
  enabled: true
  image: docker.elastic.co/beats/filebeat:8.12.0
  logstashHost: logstash-svc.logging.svc.cluster.local
  logstashPort: 5044
probes:
  liveness:
    path: /health
    initialDelaySeconds: 30
    periodSeconds: 10
  readiness:
    path: /health
    initialDelaySeconds: 10
    periodSeconds: 5
prometheus:
  scrape: true
  port: "5000"
  path: /metrics
